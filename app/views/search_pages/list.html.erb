<% provide(:title, "List") %>

<div class="center jumbotron">
<table border="1">
  <tr>
    <th>アーティスト名</th><th>公演場所</th><th>日程</th>
  </tr>

  <% @gig.each do |gig| %>
    <tr>
      <td><%= gig.artist %></td>
      <td><%= gig.place %></td>
      <td><%= gig.gig_date %></td>
    </tr>
  <% end %>
</table>
</div>
